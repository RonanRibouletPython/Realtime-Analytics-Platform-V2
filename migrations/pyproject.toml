[project]
name = "migrations"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["asyncpg>=0.29.0", "structlog>=24.1.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
